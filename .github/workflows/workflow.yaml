name: Shell commands

on: [push]

jobs:
  run-shell-command:
    runs-on: ubuntu-latest
    steps:
      - name: Echo a message
        run: echo "Hello github actions"
      - name: Multiline script
        run: |
          node -v
          npm -v